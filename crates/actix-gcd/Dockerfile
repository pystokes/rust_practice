# -------------------------------------------------------------------------------------------
# Dockerfile
#
# docker build -t rust_app .
# docker run -it --rm -p 3000:3000 -v `pwd`:/app rust_app
# -------------------------------------------------------------------------------------------

FROM rust:1.59.0

COPY . /app
EXPOSE 3000

WORKDIR /app
